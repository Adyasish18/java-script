// == vs ===

// == is only compare values and Type conversion (coercion) happens automatically 



// === is Compares value + type and no type conversion happens
// 0 == false      // true
// 0 === false     // false

// "" == false     // true
// "" === false    // false

// null == undefined // true
// null === undefined // false


// 2.🔹 What is the Ternary Operator?

// The ternary operator is a short-hand replacement for if...else.

// It’s the only operator in JavaScript that takes three operands.

// condition ? expression_if_true : expression_if_false;

// 3. What are operands?

// Operands are the values or variables on which operators work.
5+2 // 5,2 operands
